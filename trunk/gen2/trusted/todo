Now:

New Statements/expressions
 MapReduce Expression
 ForEach statement
 can the two be combined ?

Figure out how to have multi-letter variable names. 
List of reserved words.  Everything else parses as a variable. 

Port previous learning algorithms.

L8r:

Give the executor/fixer options (maybe a strategy) so that it can have variations in strategy.

Disallow MakeChangesPermanent in batch mode, since the delayed notifications may point to deleted objects.
