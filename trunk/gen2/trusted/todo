Now:

Simplify queries on construction?
Have a pattern that means false (perh. OPattern NULL).
Make queries take an OPattern

Allow queries in the links to fail. Have a violation for this. 


When the binding changes at a dynamic match, the MISSING_MATCH and EXTRA_MATCH violations become all wrong.  Fix this. 

Port previous learning algorithms.

L8r:

Give the executor/fixer options (maybe a strategy) so that it can have variations in strategy.

Disallow MakeChangesPermanent in batch mode, since the delayed notifications may point to deleted objects.
