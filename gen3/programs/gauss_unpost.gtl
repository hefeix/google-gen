{
  post (A 0)
  on (A a) if (equal (a 100000) 
     0 
     {
	post (A sum (a 1))
	    	unpost (A a) 
     }
  )
   post (B 0)
   on (A a) match (B b) {
     post (B sum (b a))
     unpost (B b)
  }
}

